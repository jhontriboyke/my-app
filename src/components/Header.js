import Logo from "../assets/Logo.svg"

function Header() {
    return (
        <header>
            <img alt="logo" src={Logo} />
        </header>
    )
}

function Hero() {

}

export default Header